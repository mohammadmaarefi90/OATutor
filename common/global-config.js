const SESSION_SYSTEM = "SEMESTER"
const SITE_NAME = "Propositional Interpretability XAI based on OATutor"
const SITE_SHORT_NAME = "PI-XAI"
const LEGACY_PLATFORM_NAME = "OATutor"
/** Stable slug for browser storage keys — do not change (preserves saved progress). */
const SITE_SLUG = "oatutor"
const CONTENT_SOURCE = "oatutor"

module.exports = {
    SESSION_SYSTEM,
    SITE_NAME,
    SITE_SHORT_NAME,
    LEGACY_PLATFORM_NAME,
    SITE_SLUG,
    CONTENT_SOURCE,
}
